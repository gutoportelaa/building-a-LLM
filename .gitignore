# Python bytecode and caches
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.dylib
*.egg-info/
build/
dist/
wheels/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
.venv-paddle/
venv/
.env
.env.*

# Editor and OS artifacts
.vscode/
.idea/
.DS_Store

# Logs and temporary runtime files
*.log
*.tmp
*.bak
logs/

# ── Saídas do pipeline (artefatos gerados, não versionados) ──────────────────

# Drop-zones e manifesto de download
territorios/
download_manifest.json

# Dados brutos de extração (todos os artefatos de extração por território)
extraidos/
dados_brutos/
dados_brutos_marker/
dados_limpos/

# Datasets de benchmark e testes com PDFs reais (binários pesados)
dados_benchmark/
teste_extractor/

# Corpus gerados (JSONL/NDJSON para fine-tuning e RAG)
corpus_*.jsonl
corpus_*.ndjson
*.ndjson
corpus_final/

# Data lake (camadas extraído/limpo/corpus/_catalog) — artefatos Parquet gerados.
# Ancorado à raiz: NÃO ignorar o pacote de código src/dompi_scraper/datalake/.
/datalake/

# Bases de treinamento por território (DBs locais pesadas)
db_treino_*/

# Dados auxiliares e resultados de scraping antigos
dados/
carnaubais_dom/
carnaubais_dom_full/
scraping_*.json
scraping_*.csv
output_pipeline/

# ── Bases vetoriais (ChromaDB / BM25) ────────────────────────────────────────
chroma_db/
chroma_db_marker/
bm25_index.pkl
embeddings_*.jsonl
progress.jsonl

# ── Amostras curadas (exceção: manter sob data/samples) ──────────────────────
data/*
!data/samples/
!data/samples/**

# Notebook checkpoints
.ipynb_checkpoints/

monitor.py
dados_brutos_orquestrador/

# Artefato de publicação no HuggingFace (regenerável a partir de datalake/corpus)
/hf_corpus_dompi/

# Área de staging do lab (logs SLURM, loghash.tsv, NDJSONs intermediários) — pesada
/staging_lab/

# Coleta de Parnaíba: stubs HTML quebrados (SPA Quasar), não são PDFs — descartável
/diarios_parnaiba/

# Configuração local do Claude Code (não compartilhada)
.claude/